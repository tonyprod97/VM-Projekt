<link rel="stylesheet/less" type="text/css" href="/css/calendar/teachers.less">
<script src="/scripts/calendar/teachers.js"></script>

<div class="backgorund-main">
    {{> calendarNav }}
<div class="container">
    <div class="row">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead class="thead-dark">
                    <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each people as |person|}}
                        <tr>
                        <th scope="row">{{person.index}}.</th>
                        <td><a onclick="sendPostForTeacher('{{person.fullName}}')">{{person.fullName}}</a></td>
                        </tr>    
                    {{/each}}
                    <tr>
                </tbody>
            </table>
        </div>
    </div>
</div>
</div>